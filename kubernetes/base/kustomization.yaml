apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: airflow

resources:
  - namespace.yaml
  - serviceaccount-airflow-worker-kubernetes-restricted.yaml
  - manifets/rendered.yaml
  - ingress.yaml
